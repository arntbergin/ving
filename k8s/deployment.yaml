apiVersion: apps/v1
kind: Deployment
metadata:
  name: ving
  namespace: ving
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ving
  template:
    metadata:
      labels:
        app: ving
    spec:
      containers:
      - name: ving
        image: ghcr.io/arntbergin/ving:latest
        ports:
        - containerPort: 8000   # må samsvare med service.targetPort
      imagePullSecrets:
      - name: ghcr-secret
